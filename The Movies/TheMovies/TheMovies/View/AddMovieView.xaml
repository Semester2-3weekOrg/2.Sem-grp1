<Page x:Class="TheMovies.View.AddMovieView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TheMovies.View"
      mc:Ignorable="d" 
      d:DesignHeight="684" d:DesignWidth="1090"
      Title="AddMovieView">

    <Grid ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock TextAlignment="Center"
                   Grid.Row="0"
                   Margin="5"
                   FontSize="24"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center">Add movie</TextBlock>
        
        <Grid Grid.Row="1" ShowGridLines="True">

            <StackPanel Margin="0,20,0,20">
                <TextBlock Text="Movie name: " 
                           Margin="0,5,0,5"/>
                <TextBox Text="{Binding MovieName}" 
                         HorizontalAlignment="Left" 
                         Width="545" 
                         Margin="0,0,0,5"/>

                <TextBlock Text="Duration: " 
                           Margin="0,5,0,5"/>
                <TextBox Text="{Binding Duration}"
                         HorizontalAlignment="Left" 
                         Width="545" 
                         Margin="0,0,0,5"/>

                <TextBlock Text="Genre: " 
                           Margin="0,5,0,5"/>
                <ComboBox ItemsSource="{Binding Genres}" 
                          HorizontalAlignment="Left" 
                          SelectedItem="{Binding SelectedGenre, Mode=TwoWay}" 
                          DisplayMemberPath="Name" 
                          Width="545" 
                          Margin="0,0,0,5"/>
                
                <TextBlock Text="Instructor: " 
                           Margin="0,5,0,5"/>
                <TextBox Text="{Binding Instructor}"
                         HorizontalAlignment="Left"
                         Width="545" 
                         Margin="0,0,0,5"/>

                <TextBlock Text="Premiere date: " 
                           Margin="0,5,0,5"/>
                <TextBox Text="{Binding PremiereDateInput}"
                         HorizontalAlignment="Left"
                         Width="545" 
                         Margin="0,0,0,5"/>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2" ShowGridLines="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" ShowGridLines="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Content="Add"
                        Command="{Binding AddMovieCommand}"
                        Width="150" 
                        Height="50"
                        Margin="10"
                        Grid.Column="3"/>
                
            </Grid>
        </Grid>
    </Grid>
</Page>
